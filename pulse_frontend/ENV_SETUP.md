# Pulse - Frontend Environment Configuration

Create a `.env.local` file in the `pulse_frontend` directory with:

```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api
```

For production, update the API URL accordingly.
